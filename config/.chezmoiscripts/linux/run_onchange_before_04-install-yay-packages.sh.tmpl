#!/usr/bin/bash

{{ if eq .osId "linux-arch" -}}
{{ $packages := list
	"spotify"
	"1password"
	"1password-cli"
	"telegram-desktop"
	"discord"
	"boomer"
	-}}

set -eufo pipefail

yay -Syu
yay -Sy {{ $packages | join " " }}

{{ end -}}
